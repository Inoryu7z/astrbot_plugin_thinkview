### v1.3.3

**✨ aiimg_generate 适配 + 中转分段发送**

**1. ✨ reasoning_only 模式下展示 aiimg_generate**

* 纯思维链模式下也会记录并展示 aiimg_generate 工具调用，args/result 完整不截断。

**2. 🐛 修复中转群长消息发送超时**

* 中转发送改为分段发送，单条不超过 1800 字，优先在换行符处切分。
* 移除中转消息的全局截断，full_agent_loop 模式不再因消息过长而发不出。

---

### v1.3.2

**🧹 精简命令与配置 + 流式修复**

**1. 🐛 修复流式模式下思考记录丢失**

* 流式响应模式下思考记录现在能正常保存。

**2. 🐛 修复 full_agent_loop 模式下内容被截断**

* 工具调用参数、结果、回复摘要和总输出不再被截断。

**3. 🧹 精简命令**

* 只保留 `/think` 和 `/think_here`。
* 移除 `/think_clear`、`/think_search` 及所有别名。

**4. 🧹 精简记录模式**

* 只保留 `reasoning_only` 和 `full_agent_loop` 两种。
* 移除定位模糊的 `reasoning_and_tools`。

**5. 🧹 最大记录数硬编码**

* 固定为 50 条，不再暴露为配置项。

---

### v1.3.1

**🐛 full_agent_loop 截断修复**

* 修复 `full_agent_loop` 模式下工具调用参数、结果、回复摘要和总输出仍被截断的问题。

---

### v1.3.0

**✨ /think_here 命令与本地输出开关**

**1. ✨ 新增 `/think_here` 命令**

* 强制将思考记录输出到当前对话，无视中转群配置。

**2. ⚙️ 新增 `force_local_output` 配置项**

* 开启后 `/think` 和 `/think_search` 的结果只发送到当前对话，不转发到中转群。
* 自动转发模式不受影响。

---

### v1.2.1

**🐛 配置加载崩溃修复**

**1. 🐛 修复配置项类型写错导致插件加载失败**

* `_conf_schema.json` 中 `integer` 回退为 `int`。

**2. ✨ 中转群格式验证**

* 拼写错误（如 `dafault`）会提前警告而非静默失败。

---

### v1.2.0

**✨ 持久化存储与命令扩展**

**1. 🐛 修复流式模式下推理内容被分隔符污染**

* 流式 chunk 现在直接拼接，多轮推理用分隔符。

**2. 🐛 修复 full_agent_loop 记录层级与 reasoning_and_tools 行为无区别**

**3. ✨ 新增命令**

* `/think_clear`：清除思考记录。
* `/think_search`：按关键词搜索记录。

**4. 💾 持久化存储**

* 记录保存到 `think_records.json`，重启不丢失。

**5. ⏱️ 命令冷却**

* 10 秒/会话冷却，防止高频调用。

**6. 🔒 用户消息脱敏**

* 非管理员查看时中间内容用 `***` 替代。

---

### v1.1.0

**✨ 稳定性优化**

**1. 🐛 修复 ID 碰撞**

* 修复同一秒多事件 ID 碰撞的问题。
* 修复同名工具连续调用时结果匹配到错误条目的问题。

**2. 🐛 修复无思考内容记录仍被存入**

* `reasoning_only` 模式下无思考内容的记录不再保存。

**3. 🧹 输出与清理优化**

* 移除"🧠 思考过程: (无思考内容)"的无意义展示。
* pending 记录自动清理（TTL 300 秒）。

**4. 🔒 安全限制**

* `/think` 命令 `n` 参数上限 20，防止查询过多记录。
* 中转群消息长度截断。

---

### v1.0.0

**🎉 初始版本**

* 查看 bot 的思考记录。
* 支持中转群配置。
* 支持管理员/普通用户权限区分。
